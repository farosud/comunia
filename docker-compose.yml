services:
  comunia:
    build: .
    env_file: .env
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./data:/app/data
      - ./agent:/app/agent
      - ./import:/app/import
    restart: unless-stopped
